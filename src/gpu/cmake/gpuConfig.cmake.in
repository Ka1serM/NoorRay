@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# gpu links Vulkan::Vulkan publicly, so consumers need it resolved.
find_dependency(Vulkan)

include("${CMAKE_CURRENT_LIST_DIR}/gpuTargets.cmake")

check_required_components(gpu)
